nelmio_api_doc:
    documentation:
        info:
            title: DataShare API
            description: API de partage de fichiers
            version: 1.0.0
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - bearerAuth: []
    areas:
        default:
            path_patterns:
                - ^/(auth|files|users)
